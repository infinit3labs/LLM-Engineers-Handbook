settings:
  docker:
    parent_image: your-registry.azurecr.io/zenml-base:latest
    skip_build: True
  orchestrator.azureml:
    synchronous: false

parameters:
  test_split_size: 0.05
  dataset_type: "preference"
  push_to_huggingface: true
  dataset_id: pauliusztin/llmtwin-dpo
  mock: false
